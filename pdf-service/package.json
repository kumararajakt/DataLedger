{
  "name": "pdf-service",
  "private": true,
  "version": "1.0.0",
  "description": "PDF parsing microservice for bank statements",
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port 5001 --reload",
    "start": "uvicorn main:app --host 0.0.0.0 --port 5001",
    "build": "echo 'No build step required for Python service'",
    "lint": "echo 'No lint step configured'",
    "clean": "rm -rf __pycache__ .pytest_cache node_modules",
    "install-deps": "pip install -r requirements.txt"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
